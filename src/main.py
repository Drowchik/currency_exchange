from server import run_server


if __name__ == "__main__":
    run_server()
    # db_name = 'currency.db'
    # a = Currencies(db_name)
    # exchange_rates = ExchangeRates(db_name)
    # exchange_rates.insert_data(0, 1, 0.99)
